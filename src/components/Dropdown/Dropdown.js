import Dropdown from 'react-dropdown';

export const DropdownComponent = (props) => {
    return (
        <>
            <Dropdown {...props} />
        </>
    )
}

// importing from library to save time but could become custom later
// also imported to customize more styles